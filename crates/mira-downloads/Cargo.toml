[package]
name = "mira-downloads"
version = "0.1.0"
edition = "2024"
description = "Shared Mira download manifests and environment selection"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
